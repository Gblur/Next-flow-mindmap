import PwdLayout from "components/password/PwdLayout";
import React, {useState} from "react";

type Props = {};

export default function pwdDashboard({}: Props) {
  // eslint-disable-next-line react-hooks/rules-of-hooks

  return (
    <>
      <PwdLayout />
    </>
  );
}
